import {  } from "../types/general";

// export const getNFTs = async (id: string) : Promise<lnft[]> => {
//   const response = await fetch(`/api/collection/${id}`);
//   const lnfts = await response.json();
//   return lnfts;
// };

// export const authenticate = async (id: string) : Promise<boolean> => {
//   const response = await fetch(`/api/collection/authenticate?id=${id}`);
//   const allowed = await response.json();
//   return allowed.allowed;
// }